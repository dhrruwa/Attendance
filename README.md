# Attendance
"A cross-platform Flutter attendance system built to kill proxy attendance. Two apps — one for students, one for teachers — use BLE proximity and short-lived rotating tokens to verify a student is physically in class, with face recognition and liveness checks for identity. Every result is server-validated via Supabase, so teachers can trust it."
