# Copy to .env and fill in. Values are passed via --dart-define at run/build time
# (see README). The anon key is publishable (gated by RLS) — safe on the client.
SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
SUPABASE_ANON_KEY=YOUR-ANON-KEY
